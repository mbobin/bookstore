#!/usr/bin/env ruby
require File.expand_path(File.join(File.dirname(__FILE__), '..', 'config', 'environment'))
FILE_EXTENSIONS = /\.pdf$|\.epub$/
listener = Listen.to("public/tmp/books", only: FILE_EXTENSIONS) do |added|
  ProcessFileJob.perform_later(added)
end
listener.start
sleep
